nav.navighation
    .navigation__wrapper 
    a.navigation__link(href="/")=meta.data.title
        svg.navigation__link__icon 

    ul.navigation__list 
        each list in navigation.data.list
            li.navigation__list__item 
                a.navigation__list__link(href=Link(list.link))=list.title
